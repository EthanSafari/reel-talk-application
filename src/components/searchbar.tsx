import React, { FC, useState, ChangeEvent } from "react";

interface SearchBarProps {
    forOnboarding: boolean
};

const SearchBar: FC<SearchBarProps> = ({ forOnboarding }) => {
    return (
        <div>

        </div>
    )
};

export default SearchBar;